import { Subtitle, Title } from '@/components/Text';
import Container from '@/components/Container';

const SettingsTab = () => (
	<Container>
		<Title>Settings</Title>
		<Subtitle>Try changing the phone appearance (dark/light)</Subtitle>
	</Container>
);

export default SettingsTab;
